name: Docker Image CI

on:
  push:
    branches: [ "appbranch" ]
  pull_request:
    branches: [ "appbranch" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag webapp:v1
 
